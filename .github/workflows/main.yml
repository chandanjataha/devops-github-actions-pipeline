# File: .github/workflows/workflow.yml

name: practice_pipeline
on:
  push:
    branches:
      - main

jobs:
  first_job:
     name: first_job
     runs-on: ubuntu-22.04
     steps:
       - run: echo "Hello first job"
  second_job:
    name: second_job
    runs-on: ubuntu-24.04
    steps:
      - run: echo "hello second job"

  third_job:
    name: third job
    runs-on: ubuntu-slim
    steps:
      - run: echo "hello 3 job"
      
